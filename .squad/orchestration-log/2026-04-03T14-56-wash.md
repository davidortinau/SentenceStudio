# Wash | 2026-04-03T14:56

**Agent:** Wash (Backend Dev)  
**Role:** Backend Development  
**Mode:** background  
**Status:** COMPLETED  

## Task

Investigate & fix GitHub issue #151 — scoring override revert bug. When the scoring override window expires, vocabulary scores should revert to pre-override state, but they were remaining in override state indefinitely.

## Outcome

**Bug Confirmed & Fixed**

- Root cause: `OverriddenScoringDto` lacked `ExpiresAt` timestamp. Override checks only verified existence, never expiration.
- Fix: Added `ExpiresAt` to `OverriddenScoringDto` schema + database migration
- Implementation: `GetVocabularyScoresAsync()` now validates expiration before returning overridden score; expired overrides are silently disregarded (clean fallback to base score)
- Persistence deferred: Overrides remain in database until after window expires. No aggressive cleanup — idempotent expiration check on read.

## Commit

Committed: `58a8364`

## Files Changed

- `src/SentenceStudio.Database/Entities/OverriddenScoring.cs` — added `ExpiresAt` property
- `src/SentenceStudio.Database/Migrations/` — new migration for `ExpiresAt` column
- `src/SentenceStudio.Entities/Dtos/OverriddenScoringDto.cs` — added `ExpiresAt` timestamp
- `src/SentenceStudio.Services/VocabularyScoreService.cs` — added expiration validation in score retrieval

## Impact

- Users: Scoring overrides now correctly expire
- API: No breaking changes; backward compatible
- Tests: All existing tests pass; no regression
